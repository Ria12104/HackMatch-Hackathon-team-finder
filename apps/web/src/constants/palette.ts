// =============================================================================
// HackMatch — Design System: Colour Palette
// =============================================================================
// All colour tokens used across the app are defined here.
//
// To change the look of the entire app, edit values in this file only.
// Components import what they need directly:
//   import { ROSE, DEEP, BG } from '@/constants/palette';
//
// Colour names reflect the mood/role, not the hex value,
// so the name stays stable even if the shade is adjusted.
// =============================================================================

/** Rose Dusk — active nav icons, logo text, inline links */
export const ROSE     = '#C38C91';

/** Deep Rose — primary CTA buttons, "Join Team", confirmed states */
export const DEEP     = '#833030';

/** Peach Veil — skill chips, selected option backgrounds, soft tags */
export const PEACH    = '#F8BCA6';

/** Golden Nectar — "Featured" badge accent (currently unused in logic) */
export const GOLDEN   = '#FDBE81';

/** Cashmere Glow — avatar backgrounds, secondary surfaces */
export const CASHMERE = '#ECDECA';

/** App Background — the main blush-white screen background */
export const BG       = '#F9EDEA';

/** Primary Text — headings and body text */
export const TEXT     = '#2D1B1E';

/** Subtitle Text — secondary labels, captions, placeholders */
export const SUBT     = '#9B7A7A';

/** Border / Divider — card outlines, separators, input borders */
export const BORDER   = '#E8D5CF';

/** Warm Tan — featured card background colour */
export const TAN      = '#C9A882';
